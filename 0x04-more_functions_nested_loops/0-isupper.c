#include "main.h"

/**
 * _isupper - Checks for an uppercase character
 * @c: character to check
 * Retur: 1 if c is uppercase, or 0 otherwise
 */
 
int _isupper(int c)
{
	int c
	
		if (c = 65 && c <= 90)
		{
		return (1);
		c++;
		}
		else
		{
		return (0);
		}
}
