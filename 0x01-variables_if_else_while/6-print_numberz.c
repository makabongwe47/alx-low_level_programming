#include <stdio.h>

/**
 * main - Entry point
 * Description: Prints all the single digit numbers
 * starting from 0
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int i = 48;

	for (i = 48; i <= 57; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
